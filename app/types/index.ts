export type Product = {
    _id: string
    id: number
    sku: string
    name: string
    description: string
    features: string[]
    gender: string
    category: string
    price: string
    sizes: string[]
    images: string[]
    status: number
}